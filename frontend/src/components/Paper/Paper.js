import React from 'react';
import Paper from '@material-ui/core/Paper';

function Paper({ children, size, color, fullWidth, ...rest }){
    return (
        <div>

        </div>
    );
}

export default Paper;